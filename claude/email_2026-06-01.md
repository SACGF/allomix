Summary:

* Implemented Wendy's new "host allele presence" method - will run alongside admixture stats model
* We need to get host/donor sex. I'm excluding chrX until then 
* Improvements to changes to VCF calling pipeline (bigger panel, better calling - see "run2_run3_run10_presence.png" (I only sent you run1/run2 the rest were internal only)

-------

WENDY'S DONOR HOM SITE - ANY HOST PRESENCE DETECTION 

* Wendy described a new method - where the donor is HOM (ref/alt), the presence of any allele from the host is evidence of the relapse. This is a great addition to the Maximum Likelihood Estimation (MLE) model, and can help at very low LOD, at the expense of it being far more error-sensitive (LoD collapses to ~14% at 1% error). So we need to be super careful about sequencing errors, barcode bleed through etc - but I think we can work those out and we should use both.

It is plotted in run2_run3_run10_presence.png alongside the MLE - (TODO: Probably insert note about differences here)

I plotted this "Per-marker host-presence structure - run 9" graph. You can see that a few locations are very high. I wanted to see whether this was real (maybe CNV?) or an artifact (mismapping, seq errors, etc) - so made a Manhattan plot. I have exported out the VCFs (attached zip) if anyone wants to look at where they are and investigate.

MANHATTAN PLOTS

I plotted the host allele % across the genome, and saw a snp in TP53 was a massive outlier for many samples. Investigating it was an indel causing a mapping error. We're in a tight place as we need to use a raw caller to let through really low AF variants through, but then lose a lot of the read/error detection, so have implemented basic filters on read strand bias etc. I re-generated it post-filter (run 10 manhattan plot now shows an X for filtered) 

I also saw the chrX plot and then realised - We need to exclude sex chromosomes for sex-mismatched pairs. 

* XLS lists siblings/relatedness, but not sex of host/donor. If someone can please add this to the XLS
* In the meantime, I will exclude chrX from analysis, and re-enable chrX on a per-patient basis if they are sex-matched

VCF CALLING ISSUES

Wendy pointed out that the panel I used had been trimmed to be close to exons, missing some intron markers - switched to using the full panel, gaining us more markers.

I had to modify the VCF calling slightly - the basics are:

* Joint call host/donor germline
* Look at allele depth at those sites in the admixture sample

But there were complexities:

* GATK sometimes holds back on a germline call thinking it's an error, but if you pass enough samples in, it can see that it's real - have passed in gnomAD >5% AF variants. This has rescued some borderline HET calls and gains us a few more markers.
* GATK has filters on very rare AD calls (as do almost all variant callers, thinking them errors) which wrecks our very low-LOD. Solution is to use "pileup" which just gives raw allele depth counts rather than any sophisticated variant calling / filtering

