"""Output formatting and HTML presentation.

The ``report`` module builds the TSV / JSON / per-marker-CSV outputs and the
report-data envelope; the ``html`` subpackage renders the self-contained HTML
reports from that envelope.
"""
